/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks/hooks.js";
import { SDKOptions, serverURLFromOptions } from "../lib/config.js";
import {
    encodeFormQuery as encodeFormQuery$,
    encodeJSON as encodeJSON$,
    encodeSimple as encodeSimple$,
} from "../lib/encodings.js";
import { HTTPClient } from "../lib/http.js";
import * as schemas$ from "../lib/schemas.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import { extractSecurity } from "../lib/security.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";

export class SDKPaymentsV1 extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    /**
     * Get server info
     */
    async paymentsgetServerInfo(
        options?: RequestOptions
    ): Promise<operations.PaymentsgetServerInfoResponse> {
        const path$ = this.templateURLComponent("/api/payments/_info")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "paymentsgetServerInfo",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.PaymentsgetServerInfoResponse>()
            .json(200, operations.PaymentsgetServerInfoResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.PaymentsgetServerInfoResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create a payment
     *
     * @remarks
     * Create a payment
     */
    async createPayment(
        request: components.PaymentRequest,
        options?: RequestOptions
    ): Promise<operations.CreatePaymentResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => components.PaymentRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$, { explode: true });

        const path$ = this.templateURLComponent("/api/payments/payments")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "createPayment",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.CreatePaymentResponse>()
            .json(200, operations.CreatePaymentResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.CreatePaymentResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List payments
     */
    async listPayments(
        request: operations.ListPaymentsRequest,
        options?: RequestOptions
    ): Promise<operations.ListPaymentsResponse> {
        const input$ = typeof request === "undefined" ? {} : request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ListPaymentsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/api/payments/payments")();

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
            query: payload$.query,
            sort: payload$.sort,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "listPayments",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListPaymentsResponse>()
            .json(200, operations.ListPaymentsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListPaymentsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get a payment
     */
    async getPayment(
        request: operations.GetPaymentRequest,
        options?: RequestOptions
    ): Promise<operations.GetPaymentResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetPaymentRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            paymentId: encodeSimple$("paymentId", payload$.paymentId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/payments/{paymentId}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getPayment",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetPaymentResponse>()
            .json(200, operations.GetPaymentResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetPaymentResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Update metadata
     */
    async updateMetadata(
        request: operations.UpdateMetadataRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.UpdateMetadataRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.RequestBody, { explode: true });

        const pathParams$ = {
            paymentId: encodeSimple$("paymentId", payload$.paymentId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/payments/{paymentId}/metadata")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "updateMetadata",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "PATCH",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * List Transfer Initiations
     */
    async listTransferInitiations(
        request: operations.ListTransferInitiationsRequest,
        options?: RequestOptions
    ): Promise<operations.ListTransferInitiationsResponse> {
        const input$ = typeof request === "undefined" ? {} : request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ListTransferInitiationsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/api/payments/transfer-initiations")();

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
            query: payload$.query,
            sort: payload$.sort,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "listTransferInitiations",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListTransferInitiationsResponse>()
            .json(200, operations.ListTransferInitiationsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListTransferInitiationsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create a TransferInitiation
     *
     * @remarks
     * Create a transfer initiation
     */
    async createTransferInitiation(
        request: components.TransferInitiationRequest,
        options?: RequestOptions
    ): Promise<operations.CreateTransferInitiationResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => components.TransferInitiationRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$, { explode: true });

        const path$ = this.templateURLComponent("/api/payments/transfer-initiations")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "createTransferInitiation",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.CreateTransferInitiationResponse>()
            .json(200, operations.CreateTransferInitiationResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.CreateTransferInitiationResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get a transfer initiation
     */
    async getTransferInitiation(
        request: operations.GetTransferInitiationRequest,
        options?: RequestOptions
    ): Promise<operations.GetTransferInitiationResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetTransferInitiationRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            transferId: encodeSimple$("transferId", payload$.transferId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/transfer-initiations/{transferId}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getTransferInitiation",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetTransferInitiationResponse>()
            .json(200, operations.GetTransferInitiationResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetTransferInitiationResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Delete a transfer initiation
     *
     * @remarks
     * Delete a transfer initiation by its id.
     */
    async deleteTransferInitiation(
        request: operations.DeleteTransferInitiationRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.DeleteTransferInitiationRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            transferId: encodeSimple$("transferId", payload$.transferId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/transfer-initiations/{transferId}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "deleteTransferInitiation",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Update the status of a transfer initiation
     *
     * @remarks
     * Update a transfer initiation status
     */
    async updateTransferInitiationStatus(
        request: operations.UpdateTransferInitiationStatusRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) =>
                operations.UpdateTransferInitiationStatusRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.UpdateTransferInitiationStatusRequest, {
            explode: true,
        });

        const pathParams$ = {
            transferId: encodeSimple$("transferId", payload$.transferId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/transfer-initiations/{transferId}/status"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "updateTransferInitiationStatus",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Reverse a transfer initiation
     *
     * @remarks
     * Reverse transfer initiation
     */
    async reverseTransferInitiation(
        request: operations.ReverseTransferInitiationRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReverseTransferInitiationRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.ReverseTransferInitiationRequest, {
            explode: true,
        });

        const pathParams$ = {
            transferId: encodeSimple$("transferId", payload$.transferId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/transfer-initiations/{transferId}/reverse"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "reverseTransferInitiation",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Retry a failed transfer initiation
     *
     * @remarks
     * Retry a failed transfer initiation
     */
    async retryTransferInitiation(
        request: operations.RetryTransferInitiationRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.RetryTransferInitiationRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            transferId: encodeSimple$("transferId", payload$.transferId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/transfer-initiations/{transferId}/retry"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "retryTransferInitiation",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * List Pools
     */
    async listPools(
        request: operations.ListPoolsRequest,
        options?: RequestOptions
    ): Promise<operations.ListPoolsResponse> {
        const input$ = typeof request === "undefined" ? {} : request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ListPoolsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/api/payments/pools")();

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
            query: payload$.query,
            sort: payload$.sort,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "listPools",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListPoolsResponse>()
            .json(200, operations.ListPoolsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListPoolsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create a Pool
     *
     * @remarks
     * Create a Pool
     */
    async createPool(
        request: components.PoolRequest,
        options?: RequestOptions
    ): Promise<operations.CreatePoolResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => components.PoolRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$, { explode: true });

        const path$ = this.templateURLComponent("/api/payments/pools")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "createPool",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.CreatePoolResponse>()
            .json(200, operations.CreatePoolResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.CreatePoolResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get a Pool
     */
    async getPool(
        request: operations.GetPoolRequest,
        options?: RequestOptions
    ): Promise<operations.GetPoolResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetPoolRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            poolId: encodeSimple$("poolId", payload$.poolId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/pools/{poolId}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getPool",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetPoolResponse>()
            .json(200, operations.GetPoolResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetPoolResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Delete a Pool
     *
     * @remarks
     * Delete a pool by its id.
     */
    async deletePool(
        request: operations.DeletePoolRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.DeletePoolRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            poolId: encodeSimple$("poolId", payload$.poolId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/pools/{poolId}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "deletePool",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Add an account to a pool
     *
     * @remarks
     * Add an account to a pool
     */
    async addAccountToPool(
        request: operations.AddAccountToPoolRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.AddAccountToPoolRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.AddAccountToPoolRequest, { explode: true });

        const pathParams$ = {
            poolId: encodeSimple$("poolId", payload$.poolId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/pools/{poolId}/accounts")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "addAccountToPool",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Remove an account from a pool
     *
     * @remarks
     * Remove an account from a pool by its id.
     */
    async removeAccountFromPool(
        request: operations.RemoveAccountFromPoolRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.RemoveAccountFromPoolRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            accountId: encodeSimple$("accountId", payload$.accountId, {
                explode: false,
                charEncoding: "percent",
            }),
            poolId: encodeSimple$("poolId", payload$.poolId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/pools/{poolId}/accounts/{accountId}"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "removeAccountFromPool",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Get historical pool balances at a particular point in time
     */
    async getPoolBalances(
        request: operations.GetPoolBalancesRequest,
        options?: RequestOptions
    ): Promise<operations.GetPoolBalancesResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetPoolBalancesRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            poolId: encodeSimple$("poolId", payload$.poolId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/pools/{poolId}/balances")(
            pathParams$
        );

        const query$ = encodeFormQuery$({
            at: payload$.at,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getPoolBalances",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetPoolBalancesResponse>()
            .json(200, operations.GetPoolBalancesResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetPoolBalancesResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get latest pool balances
     */
    async getPoolBalancesLatest(
        request: operations.GetPoolBalancesLatestRequest,
        options?: RequestOptions
    ): Promise<operations.GetPoolBalancesLatestResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetPoolBalancesLatestRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            poolId: encodeSimple$("poolId", payload$.poolId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/pools/{poolId}/balances/latest")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getPoolBalancesLatest",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetPoolBalancesLatestResponse>()
            .json(200, operations.GetPoolBalancesLatestResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetPoolBalancesLatestResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create an account
     *
     * @remarks
     * Create an account
     */
    async createAccount(
        request: components.AccountRequest,
        options?: RequestOptions
    ): Promise<operations.CreateAccountResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => components.AccountRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$, { explode: true });

        const path$ = this.templateURLComponent("/api/payments/accounts")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "createAccount",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.CreateAccountResponse>()
            .json(200, operations.CreateAccountResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.CreateAccountResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List accounts
     */
    async paymentslistAccounts(
        request: operations.PaymentslistAccountsRequest,
        options?: RequestOptions
    ): Promise<operations.PaymentslistAccountsResponse> {
        const input$ = typeof request === "undefined" ? {} : request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.PaymentslistAccountsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/api/payments/accounts")();

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
            query: payload$.query,
            sort: payload$.sort,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "paymentslistAccounts",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.PaymentslistAccountsResponse>()
            .json(200, operations.PaymentslistAccountsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.PaymentslistAccountsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get an account
     */
    async paymentsgetAccount(
        request: operations.PaymentsgetAccountRequest,
        options?: RequestOptions
    ): Promise<operations.PaymentsgetAccountResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.PaymentsgetAccountRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            accountId: encodeSimple$("accountId", payload$.accountId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/accounts/{accountId}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "paymentsgetAccount",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.PaymentsgetAccountResponse>()
            .json(200, operations.PaymentsgetAccountResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.PaymentsgetAccountResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get account balances
     */
    async getAccountBalances(
        request: operations.GetAccountBalancesRequest,
        options?: RequestOptions
    ): Promise<operations.GetAccountBalancesResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetAccountBalancesRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            accountId: encodeSimple$("accountId", payload$.accountId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/accounts/{accountId}/balances")(
            pathParams$
        );

        const query$ = encodeFormQuery$({
            asset: payload$.asset,
            cursor: payload$.cursor,
            from: payload$.from,
            limit: payload$.limit,
            pageSize: payload$.pageSize,
            sort: payload$.sort,
            to: payload$.to,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getAccountBalances",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetAccountBalancesResponse>()
            .json(200, operations.GetAccountBalancesResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetAccountBalancesResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create a BankAccount in Payments and on the PSP
     *
     * @remarks
     * Create a bank account in Payments and on the PSP.
     */
    async createBankAccount(
        request: components.BankAccountRequest,
        options?: RequestOptions
    ): Promise<operations.CreateBankAccountResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => components.BankAccountRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$, { explode: true });

        const path$ = this.templateURLComponent("/api/payments/bank-accounts")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "createBankAccount",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.CreateBankAccountResponse>()
            .json(200, operations.CreateBankAccountResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.CreateBankAccountResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List bank accounts created by user on Formance
     *
     * @remarks
     * List all bank accounts created by user on Formance.
     */
    async listBankAccounts(
        request: operations.ListBankAccountsRequest,
        options?: RequestOptions
    ): Promise<operations.ListBankAccountsResponse> {
        const input$ = typeof request === "undefined" ? {} : request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ListBankAccountsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/api/payments/bank-accounts")();

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
            sort: payload$.sort,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "listBankAccounts",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListBankAccountsResponse>()
            .json(200, operations.ListBankAccountsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListBankAccountsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get a bank account created by user on Formance
     */
    async getBankAccount(
        request: operations.GetBankAccountRequest,
        options?: RequestOptions
    ): Promise<operations.GetBankAccountResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetBankAccountRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            bankAccountId: encodeSimple$("bankAccountId", payload$.bankAccountId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/bank-accounts/{bankAccountId}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getBankAccount",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetBankAccountResponse>()
            .json(200, operations.GetBankAccountResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetBankAccountResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Forward a bank account to a connector
     */
    async forwardBankAccount(
        request: operations.ForwardBankAccountRequest,
        options?: RequestOptions
    ): Promise<operations.ForwardBankAccountResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ForwardBankAccountRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.ForwardBankAccountRequest, { explode: true });

        const pathParams$ = {
            bankAccountId: encodeSimple$("bankAccountId", payload$.bankAccountId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/bank-accounts/{bankAccountId}/forward"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "forwardBankAccount",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ForwardBankAccountResponse>()
            .json(200, operations.ForwardBankAccountResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ForwardBankAccountResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Update metadata of a bank account
     */
    async updateBankAccountMetadata(
        request: operations.UpdateBankAccountMetadataRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.UpdateBankAccountMetadataRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.UpdateBankAccountMetadataRequest, {
            explode: true,
        });

        const pathParams$ = {
            bankAccountId: encodeSimple$("bankAccountId", payload$.bankAccountId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/bank-accounts/{bankAccountId}/metadata"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "updateBankAccountMetadata",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "PATCH",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * List all installed connectors
     *
     * @remarks
     * List all installed connectors.
     */
    async listAllConnectors(
        options?: RequestOptions
    ): Promise<operations.ListAllConnectorsResponse> {
        const path$ = this.templateURLComponent("/api/payments/connectors")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "listAllConnectors",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListAllConnectorsResponse>()
            .json(200, operations.ListAllConnectorsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListAllConnectorsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List the configs of each available connector
     *
     * @remarks
     * List the configs of each available connector.
     */
    async listConfigsAvailableConnectors(
        options?: RequestOptions
    ): Promise<operations.ListConfigsAvailableConnectorsResponse> {
        const path$ = this.templateURLComponent("/api/payments/connectors/configs")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "listConfigsAvailableConnectors",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListConfigsAvailableConnectorsResponse>()
            .json(200, operations.ListConfigsAvailableConnectorsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListConfigsAvailableConnectorsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Install a connector
     *
     * @remarks
     * Install a connector by its name and config.
     */
    async installConnector(
        request: operations.InstallConnectorRequest,
        options?: RequestOptions
    ): Promise<operations.InstallConnectorResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.InstallConnectorRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.ConnectorConfig, { explode: true });

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/connectors/{connector}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "installConnector",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.InstallConnectorResponse>()
            .json(201, operations.InstallConnectorResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.InstallConnectorResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Uninstall a connector
     *
     * @remarks
     * Uninstall a connector by its name.
     *
     * @deprecated method: This will be removed in a future release, please migrate away from it as soon as possible.
     */
    async uninstallConnector(
        request: operations.UninstallConnectorRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.UninstallConnectorRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/connectors/{connector}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "uninstallConnector",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Uninstall a connector
     *
     * @remarks
     * Uninstall a connector by its name.
     */
    async uninstallConnectorV1(
        request: operations.UninstallConnectorV1Request,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.UninstallConnectorV1Request$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
            connectorId: encodeSimple$("connectorId", payload$.connectorId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/connectors/{connector}/{connectorId}"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "uninstallConnectorV1",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Read the config of a connector
     *
     * @remarks
     * Read connector config
     *
     * @deprecated method: This will be removed in a future release, please migrate away from it as soon as possible.
     */
    async readConnectorConfig(
        request: operations.ReadConnectorConfigRequest,
        options?: RequestOptions
    ): Promise<operations.ReadConnectorConfigResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadConnectorConfigRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/connectors/{connector}/config")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "readConnectorConfig",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadConnectorConfigResponse>()
            .json(200, operations.ReadConnectorConfigResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadConnectorConfigResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Update the config of a connector
     *
     * @remarks
     * Update connector config
     */
    async updateConnectorConfigV1(
        request: operations.UpdateConnectorConfigV1Request,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.UpdateConnectorConfigV1Request$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.ConnectorConfig, { explode: true });

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
            connectorId: encodeSimple$("connectorId", payload$.connectorId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/connectors/{connector}/{connectorId}/config"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "updateConnectorConfigV1",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Read the config of a connector
     *
     * @remarks
     * Read connector config
     */
    async readConnectorConfigV1(
        request: operations.ReadConnectorConfigV1Request,
        options?: RequestOptions
    ): Promise<operations.ReadConnectorConfigV1Response> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadConnectorConfigV1Request$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
            connectorId: encodeSimple$("connectorId", payload$.connectorId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/connectors/{connector}/{connectorId}/config"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "readConnectorConfigV1",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadConnectorConfigV1Response>()
            .json(200, operations.ReadConnectorConfigV1Response$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadConnectorConfigV1Response$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Reset a connector
     *
     * @remarks
     * Reset a connector by its name.
     * It will remove the connector and ALL PAYMENTS generated with it.
     *
     *
     * @deprecated method: This will be removed in a future release, please migrate away from it as soon as possible.
     */
    async resetConnector(
        request: operations.ResetConnectorRequest,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ResetConnectorRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/connectors/{connector}/reset")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "resetConnector",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Reset a connector
     *
     * @remarks
     * Reset a connector by its name.
     * It will remove the connector and ALL PAYMENTS generated with it.
     *
     */
    async resetConnectorV1(
        request: operations.ResetConnectorV1Request,
        options?: RequestOptions
    ): Promise<components.PaymentsErrorResponse | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ResetConnectorV1Request$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
            connectorId: encodeSimple$("connectorId", payload$.connectorId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/connectors/{connector}/{connectorId}/reset"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "resetConnectorV1",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.PaymentsErrorResponse | undefined>()
            .void(204, components.PaymentsErrorResponse$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.PaymentsErrorResponse$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * List tasks from a connector
     *
     * @remarks
     * List all tasks associated with this connector.
     *
     * @deprecated method: This will be removed in a future release, please migrate away from it as soon as possible.
     */
    async listConnectorTasks(
        request: operations.ListConnectorTasksRequest,
        options?: RequestOptions
    ): Promise<operations.ListConnectorTasksResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ListConnectorTasksRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/connectors/{connector}/tasks")(
            pathParams$
        );

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "listConnectorTasks",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListConnectorTasksResponse>()
            .json(200, operations.ListConnectorTasksResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListConnectorTasksResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List tasks from a connector
     *
     * @remarks
     * List all tasks associated with this connector.
     */
    async listConnectorTasksV1(
        request: operations.ListConnectorTasksV1Request,
        options?: RequestOptions
    ): Promise<operations.ListConnectorTasksV1Response> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ListConnectorTasksV1Request$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
            connectorId: encodeSimple$("connectorId", payload$.connectorId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/connectors/{connector}/{connectorId}/tasks"
        )(pathParams$);

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "listConnectorTasksV1",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListConnectorTasksV1Response>()
            .json(200, operations.ListConnectorTasksV1Response$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListConnectorTasksV1Response$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Read a specific task of the connector
     *
     * @remarks
     * Get a specific task associated to the connector.
     *
     * @deprecated method: This will be removed in a future release, please migrate away from it as soon as possible.
     */
    async getConnectorTask(
        request: operations.GetConnectorTaskRequest,
        options?: RequestOptions
    ): Promise<operations.GetConnectorTaskResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetConnectorTaskRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
            taskId: encodeSimple$("taskId", payload$.taskId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/connectors/{connector}/tasks/{taskId}"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getConnectorTask",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetConnectorTaskResponse>()
            .json(200, operations.GetConnectorTaskResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetConnectorTaskResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Read a specific task of the connector
     *
     * @remarks
     * Get a specific task associated to the connector.
     */
    async getConnectorTaskV1(
        request: operations.GetConnectorTaskV1Request,
        options?: RequestOptions
    ): Promise<operations.GetConnectorTaskV1Response> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetConnectorTaskV1Request$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
            connectorId: encodeSimple$("connectorId", payload$.connectorId, {
                explode: false,
                charEncoding: "percent",
            }),
            taskId: encodeSimple$("taskId", payload$.taskId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/payments/connectors/{connector}/{connectorId}/tasks/{taskId}"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getConnectorTaskV1",
            oAuth2Scopes: ["auth:read", "payments:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetConnectorTaskV1Response>()
            .json(200, operations.GetConnectorTaskV1Response$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetConnectorTaskV1Response$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Transfer funds between Connector accounts
     *
     * @remarks
     * Execute a transfer between two accounts.
     */
    async connectorsTransfer(
        request: operations.ConnectorsTransferRequest,
        options?: RequestOptions
    ): Promise<operations.ConnectorsTransferResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ConnectorsTransferRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.TransferRequest, { explode: true });

        const pathParams$ = {
            connector: encodeSimple$("connector", payload$.connector, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/payments/connectors/{connector}/transfers")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "connectorsTransfer",
            oAuth2Scopes: ["auth:read", "payments:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ConnectorsTransferResponse>()
            .json(200, operations.ConnectorsTransferResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ConnectorsTransferResponse$inboundSchema)
            .match(response);

        return result$;
    }
}
