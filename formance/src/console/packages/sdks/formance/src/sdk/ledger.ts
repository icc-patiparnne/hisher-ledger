/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks/hooks.js";
import { SDKOptions, serverURLFromOptions } from "../lib/config.js";
import { HTTPClient } from "../lib/http.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import { extractSecurity } from "../lib/security.js";
import * as errors from "../models/errors/index.js";
import * as operations from "../models/operations/index.js";
import { Sdkv1 } from "./sdkv1.js";
import { V2 } from "./v2.js";

export class Ledger extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    private _v1?: Sdkv1;
    get v1(): Sdkv1 {
        return (this._v1 ??= new Sdkv1(this.options$));
    }

    private _v2?: V2;
    get v2(): V2 {
        return (this._v2 ??= new V2(this.options$));
    }

    /**
     * Show server information
     */
    async getInfo(options?: RequestOptions): Promise<operations.V2GetInfoResponse> {
        const path$ = this.templateURLComponent("/api/ledger/_/info")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2GetInfo",
            oAuth2Scopes: ["auth:read", "ledger:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const responseFields$ = {
            HttpMeta: { Response: response, Request: request$ },
        };

        const [result$] = await this.matcher<operations.V2GetInfoResponse>()
            .json(200, operations.V2GetInfoResponse$inboundSchema)
            .fail("4XX")
            .json("5XX", errors.V2ErrorResponse$inboundSchema, { err: true })
            .json("default", operations.V2GetInfoResponse$inboundSchema)
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    /**
     * Read in memory metrics
     */
    async getMetrics(options?: RequestOptions): Promise<operations.GetMetricsResponse> {
        const path$ = this.templateURLComponent("/api/ledger/_/metrics")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "getMetrics",
            oAuth2Scopes: ["auth:read", "ledger:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetMetricsResponse>()
            .json(200, operations.GetMetricsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetMetricsResponse$inboundSchema)
            .match(response);

        return result$;
    }
}
