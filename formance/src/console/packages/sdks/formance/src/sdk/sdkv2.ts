/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks/hooks.js";
import { SDKOptions, serverURLFromOptions } from "../lib/config.js";
import {
    encodeFormQuery as encodeFormQuery$,
    encodeJSON as encodeJSON$,
    encodeSimple as encodeSimple$,
} from "../lib/encodings.js";
import { HTTPClient } from "../lib/http.js";
import * as schemas$ from "../lib/schemas.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import { extractSecurity } from "../lib/security.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";

export class Sdkv2 extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    /**
     * Get server info
     */
    async getServerInfo(options?: RequestOptions): Promise<operations.V2GetServerInfoResponse> {
        const path$ = this.templateURLComponent("/api/orchestration/v2/_info")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2GetServerInfo",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2GetServerInfoResponse>()
            .json(200, operations.V2GetServerInfoResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2GetServerInfoResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List triggers
     *
     * @remarks
     * List triggers
     */
    async listTriggers(
        request: operations.V2ListTriggersRequest,
        options?: RequestOptions
    ): Promise<operations.V2ListTriggersResponse> {
        const input$ = typeof request === "undefined" ? {} : request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2ListTriggersRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/api/orchestration/v2/triggers")();

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            name: payload$.name,
            pageSize: payload$.pageSize,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2ListTriggers",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2ListTriggersResponse>()
            .json(200, operations.V2ListTriggersResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2ListTriggersResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create trigger
     *
     * @remarks
     * Create trigger
     */
    async createTrigger(
        request?: components.V2TriggerData | undefined,
        options?: RequestOptions
    ): Promise<operations.V2CreateTriggerResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => components.V2TriggerData$outboundSchema.optional().parse(value$),
            "Input validation failed"
        );
        const body$ =
            payload$ === undefined ? null : encodeJSON$("body", payload$, { explode: true });

        const path$ = this.templateURLComponent("/api/orchestration/v2/triggers")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2CreateTrigger",
            oAuth2Scopes: ["auth:read", "orchestration:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2CreateTriggerResponse>()
            .json(201, operations.V2CreateTriggerResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2CreateTriggerResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Read trigger
     *
     * @remarks
     * Read trigger
     */
    async readTrigger(
        request: operations.V2ReadTriggerRequest,
        options?: RequestOptions
    ): Promise<operations.V2ReadTriggerResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2ReadTriggerRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            triggerID: encodeSimple$("triggerID", payload$.triggerID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/orchestration/v2/triggers/{triggerID}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2ReadTrigger",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2ReadTriggerResponse>()
            .json(200, operations.V2ReadTriggerResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2ReadTriggerResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Delete trigger
     *
     * @remarks
     * Read trigger
     */
    async deleteTrigger(
        request: operations.V2DeleteTriggerRequest,
        options?: RequestOptions
    ): Promise<components.V2Error | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2DeleteTriggerRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            triggerID: encodeSimple$("triggerID", payload$.triggerID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/orchestration/v2/triggers/{triggerID}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2DeleteTrigger",
            oAuth2Scopes: ["auth:read", "orchestration:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.V2Error | undefined>()
            .void(204, components.V2Error$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.V2Error$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Test trigger
     *
     * @remarks
     * Test trigger
     */
    async testTrigger(
        request: operations.TestTriggerRequest,
        options?: RequestOptions
    ): Promise<operations.TestTriggerResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.TestTriggerRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.RequestBody, { explode: true });

        const pathParams$ = {
            triggerID: encodeSimple$("triggerID", payload$.triggerID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/orchestration/v2/triggers/{triggerID}/test")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "testTrigger",
            oAuth2Scopes: ["auth:read", "orchestration:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.TestTriggerResponse>()
            .json(200, operations.TestTriggerResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.TestTriggerResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List triggers occurrences
     *
     * @remarks
     * List triggers occurrences
     */
    async listTriggersOccurrences(
        request: operations.V2ListTriggersOccurrencesRequest,
        options?: RequestOptions
    ): Promise<operations.V2ListTriggersOccurrencesResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2ListTriggersOccurrencesRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            triggerID: encodeSimple$("triggerID", payload$.triggerID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/orchestration/v2/triggers/{triggerID}/occurrences"
        )(pathParams$);

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2ListTriggersOccurrences",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2ListTriggersOccurrencesResponse>()
            .json(200, operations.V2ListTriggersOccurrencesResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2ListTriggersOccurrencesResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List registered workflows
     *
     * @remarks
     * List registered workflows
     */
    async listWorkflows(
        request: operations.V2ListWorkflowsRequest,
        options?: RequestOptions
    ): Promise<operations.V2ListWorkflowsResponse> {
        const input$ = typeof request === "undefined" ? {} : request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2ListWorkflowsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/api/orchestration/v2/workflows")();

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2ListWorkflows",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2ListWorkflowsResponse>()
            .json(200, operations.V2ListWorkflowsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2ListWorkflowsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create workflow
     *
     * @remarks
     * Create a workflow
     */
    async createWorkflow(
        request?: components.V2CreateWorkflowRequest | undefined,
        options?: RequestOptions
    ): Promise<operations.V2CreateWorkflowResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => components.V2CreateWorkflowRequest$outboundSchema.optional().parse(value$),
            "Input validation failed"
        );
        const body$ =
            payload$ === undefined ? null : encodeJSON$("body", payload$, { explode: true });

        const path$ = this.templateURLComponent("/api/orchestration/v2/workflows")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2CreateWorkflow",
            oAuth2Scopes: ["auth:read", "orchestration:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2CreateWorkflowResponse>()
            .json(201, operations.V2CreateWorkflowResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2CreateWorkflowResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get a flow by id
     *
     * @remarks
     * Get a flow by id
     */
    async getWorkflow(
        request: operations.V2GetWorkflowRequest,
        options?: RequestOptions
    ): Promise<operations.V2GetWorkflowResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2GetWorkflowRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            flowId: encodeSimple$("flowId", payload$.flowId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/orchestration/v2/workflows/{flowId}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2GetWorkflow",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2GetWorkflowResponse>()
            .json(200, operations.V2GetWorkflowResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2GetWorkflowResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Delete a flow by id
     *
     * @remarks
     * Delete a flow by id
     */
    async deleteWorkflow(
        request: operations.V2DeleteWorkflowRequest,
        options?: RequestOptions
    ): Promise<components.V2Error | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2DeleteWorkflowRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            flowId: encodeSimple$("flowId", payload$.flowId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/orchestration/v2/workflows/{flowId}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2DeleteWorkflow",
            oAuth2Scopes: ["auth:read", "orchestration:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.V2Error | undefined>()
            .void(204, components.V2Error$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.V2Error$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Run workflow
     *
     * @remarks
     * Run workflow
     */
    async runWorkflow(
        request: operations.V2RunWorkflowRequest,
        options?: RequestOptions
    ): Promise<operations.V2RunWorkflowResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2RunWorkflowRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.RequestBody, { explode: true });

        const pathParams$ = {
            workflowID: encodeSimple$("workflowID", payload$.workflowID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/orchestration/v2/workflows/{workflowID}/instances"
        )(pathParams$);

        const query$ = encodeFormQuery$({
            wait: payload$.wait,
        });

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2RunWorkflow",
            oAuth2Scopes: ["auth:read", "orchestration:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2RunWorkflowResponse>()
            .json(201, operations.V2RunWorkflowResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2RunWorkflowResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * List instances of a workflow
     *
     * @remarks
     * List instances of a workflow
     */
    async listInstances(
        request: operations.V2ListInstancesRequest,
        options?: RequestOptions
    ): Promise<operations.V2ListInstancesResponse> {
        const input$ = typeof request === "undefined" ? {} : request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2ListInstancesRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/api/orchestration/v2/instances")();

        const query$ = encodeFormQuery$({
            cursor: payload$.cursor,
            pageSize: payload$.pageSize,
            running: payload$.running,
            workflowID: payload$.workflowID,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2ListInstances",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2ListInstancesResponse>()
            .json(200, operations.V2ListInstancesResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2ListInstancesResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get a workflow instance by id
     *
     * @remarks
     * Get a workflow instance by id
     */
    async getInstance(
        request: operations.V2GetInstanceRequest,
        options?: RequestOptions
    ): Promise<operations.V2GetInstanceResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2GetInstanceRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            instanceID: encodeSimple$("instanceID", payload$.instanceID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/api/orchestration/v2/instances/{instanceID}")(
            pathParams$
        );

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2GetInstance",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2GetInstanceResponse>()
            .json(200, operations.V2GetInstanceResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2GetInstanceResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Send an event to a running workflow
     *
     * @remarks
     * Send an event to a running workflow
     */
    async sendEvent(
        request: operations.V2SendEventRequest,
        options?: RequestOptions
    ): Promise<components.V2Error | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2SendEventRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.RequestBody, { explode: true });

        const pathParams$ = {
            instanceID: encodeSimple$("instanceID", payload$.instanceID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/orchestration/v2/instances/{instanceID}/events"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2SendEvent",
            oAuth2Scopes: ["auth:read", "orchestration:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.V2Error | undefined>()
            .void(204, components.V2Error$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.V2Error$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Cancel a running workflow
     *
     * @remarks
     * Cancel a running workflow
     */
    async cancelEvent(
        request: operations.V2CancelEventRequest,
        options?: RequestOptions
    ): Promise<components.V2Error | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2CancelEventRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            instanceID: encodeSimple$("instanceID", payload$.instanceID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/orchestration/v2/instances/{instanceID}/abort"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2CancelEvent",
            oAuth2Scopes: ["auth:read", "orchestration:write"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "PUT",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.V2Error | undefined>()
            .void(204, components.V2Error$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.V2Error$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Get a workflow instance history by id
     *
     * @remarks
     * Get a workflow instance history by id
     */
    async getInstanceHistory(
        request: operations.V2GetInstanceHistoryRequest,
        options?: RequestOptions
    ): Promise<operations.V2GetInstanceHistoryResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2GetInstanceHistoryRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            instanceID: encodeSimple$("instanceID", payload$.instanceID, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/orchestration/v2/instances/{instanceID}/history"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2GetInstanceHistory",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2GetInstanceHistoryResponse>()
            .json(200, operations.V2GetInstanceHistoryResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2GetInstanceHistoryResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get a workflow instance stage history
     *
     * @remarks
     * Get a workflow instance stage history
     */
    async getInstanceStageHistory(
        request: operations.V2GetInstanceStageHistoryRequest,
        options?: RequestOptions
    ): Promise<operations.V2GetInstanceStageHistoryResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.V2GetInstanceStageHistoryRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            instanceID: encodeSimple$("instanceID", payload$.instanceID, {
                explode: false,
                charEncoding: "percent",
            }),
            number: encodeSimple$("number", payload$.number, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent(
            "/api/orchestration/v2/instances/{instanceID}/stages/{number}/history"
        )(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "v2GetInstanceStageHistory",
            oAuth2Scopes: ["auth:read", "orchestration:read"],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.V2GetInstanceStageHistoryResponse>()
            .json(200, operations.V2GetInstanceStageHistoryResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.V2GetInstanceStageHistoryResponse$inboundSchema)
            .match(response);

        return result$;
    }
}
