/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks/hooks.js";
import { SDKOptions, serverURLFromOptions } from "../lib/config.js";
import {
    encodeFormQuery as encodeFormQuery$,
    encodeJSON as encodeJSON$,
    encodeSimple as encodeSimple$,
} from "../lib/encodings.js";
import { HTTPClient } from "../lib/http.js";
import * as schemas$ from "../lib/schemas.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";

export enum ReadAppManifestAcceptEnum {
    applicationJson = "application/json",
    applicationOctetStream = "application/octet-stream",
}

export enum DownloadAppVersionAcceptEnum {
    applicationJson = "application/json",
    applicationGzip = "application/gzip",
}

export class SDK extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    /**
     * List organization apps
     */
    async listApps(
        request: operations.ListAppsRequest,
        options?: RequestOptions
    ): Promise<operations.ListAppsResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ListAppsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const path$ = this.templateURLComponent("/apps")();

        const query$ = encodeFormQuery$({
            organizationId: payload$.organizationId,
            pageNumber: payload$.pageNumber,
            pageSize: payload$.pageSize,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = { operationID: "listApps", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ListAppsResponse>()
            .json(200, operations.ListAppsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ListAppsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create a new app
     */
    async createApp(
        request: components.CreateAppRequest,
        options?: RequestOptions
    ): Promise<operations.CreateAppResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => components.CreateAppRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$, { explode: true });

        const path$ = this.templateURLComponent("/apps")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const context = { operationID: "createApp", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.CreateAppResponse>()
            .json(201, operations.CreateAppResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.CreateAppResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Update an app
     */
    async updateApp(
        request: operations.UpdateAppRequest,
        options?: RequestOptions
    ): Promise<components.ErrorT | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.UpdateAppRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.UpdateAppRequest, { explode: true });

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const context = { operationID: "updateApp", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "PUT",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.ErrorT | undefined>()
            .void(204, components.ErrorT$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.ErrorT$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * read app details
     */
    async readApp(
        request: operations.ReadAppRequest,
        options?: RequestOptions
    ): Promise<operations.ReadAppResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadAppRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = { operationID: "readApp", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadAppResponse>()
            .json(200, operations.ReadAppResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadAppResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Delete an app
     */
    async deleteApp(
        request: operations.DeleteAppRequest,
        options?: RequestOptions
    ): Promise<components.ErrorT | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.DeleteAppRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = { operationID: "deleteApp", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.ErrorT | undefined>()
            .void(204, components.ErrorT$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.ErrorT$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Get the current state version of an app
     */
    async getAppCurrentStateVersion(
        request: operations.GetAppCurrentStateVersionRequest,
        options?: RequestOptions
    ): Promise<operations.GetAppCurrentStateVersionResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetAppCurrentStateVersionRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/current-state-version")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = {
            operationID: "getAppCurrentStateVersion",
            oAuth2Scopes: [],
            securitySource: null,
        };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetAppCurrentStateVersionResponse>()
            .json(200, operations.GetAppCurrentStateVersionResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetAppCurrentStateVersionResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get all variables of an app
     */
    async readAppVariables(
        request: operations.ReadAppVariablesRequest,
        options?: RequestOptions
    ): Promise<operations.ReadAppVariablesResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadAppVariablesRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/variables")(pathParams$);

        const query$ = encodeFormQuery$({
            pageNumber: payload$.pageNumber,
            pageSize: payload$.pageSize,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = { operationID: "readAppVariables", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadAppVariablesResponse>()
            .json(200, operations.ReadAppVariablesResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadAppVariablesResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Create variable for an app
     */
    async createAppVariable(
        request: operations.CreateAppVariableRequest,
        options?: RequestOptions
    ): Promise<operations.CreateAppVariableResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.CreateAppVariableRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.CreateVariableRequest, { explode: true });

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/variables")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const context = {
            operationID: "createAppVariable",
            oAuth2Scopes: [],
            securitySource: null,
        };

        const request$ = this.createRequest$(
            context,
            {
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.CreateAppVariableResponse>()
            .json(201, operations.CreateAppVariableResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.CreateAppVariableResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Delete a variable from an app
     */
    async deleteAppVariable(
        request: operations.DeleteAppVariableRequest,
        options?: RequestOptions
    ): Promise<components.ErrorT | undefined> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.DeleteAppVariableRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
            variableId: encodeSimple$("variableId", payload$.variableId, {
                explode: false,
                charEncoding: "percent",
            }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/variables/{variableId}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = {
            operationID: "deleteAppVariable",
            oAuth2Scopes: [],
            securitySource: null,
        };

        const request$ = this.createRequest$(
            context,
            {
                method: "DELETE",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<components.ErrorT | undefined>()
            .void(204, components.ErrorT$inboundSchema.optional())
            .fail(["4XX", "5XX"])
            .json("default", components.ErrorT$inboundSchema.optional())
            .match(response);

        return result$;
    }

    /**
     * Get versions of an app
     */
    async readAppVersions(
        request: operations.ReadAppVersionsRequest,
        options?: RequestOptions
    ): Promise<operations.ReadAppVersionsResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadAppVersionsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/versions")(pathParams$);

        const query$ = encodeFormQuery$({
            pageNumber: payload$.pageNumber,
            pageSize: payload$.pageSize,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = { operationID: "readAppVersions", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadAppVersionsResponse>()
            .json(200, operations.ReadAppVersionsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadAppVersionsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get the last valid deployed manifest of an app
     */
    async readAppManifest(
        request: operations.ReadAppManifestRequest,
        options?: RequestOptions & { acceptHeaderOverride?: ReadAppManifestAcceptEnum }
    ): Promise<operations.ReadAppManifestResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadAppManifestRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/manifest")(pathParams$);

        const query$ = encodeFormQuery$({
            from: payload$.from,
        });

        const headers$ = new Headers({
            Accept:
                options?.acceptHeaderOverride ||
                "application/json;q=1, application/octet-stream;q=0",
        });

        const context = { operationID: "readAppManifest", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadAppManifestResponse>()
            .stream(200, operations.ReadAppManifestResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadAppManifestResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Deploy a new configuration for an app
     */
    async deployAppConfigurationJson(
        request: operations.DeployAppConfigurationJsonRequest,
        options?: RequestOptions
    ): Promise<operations.DeployAppConfigurationJsonResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.DeployAppConfigurationJsonRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.application, { explode: true });

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/deploy")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const context = {
            operationID: "deployAppConfiguration_json",
            oAuth2Scopes: [],
            securitySource: null,
        };

        const request$ = this.createRequest$(
            context,
            {
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.DeployAppConfigurationJsonResponse>()
            .json(202, operations.DeployAppConfigurationJsonResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.DeployAppConfigurationJsonResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Deploy a new configuration for an app
     */
    async deployAppConfigurationRaw(
        request: operations.DeployAppConfigurationRawRequest,
        options?: RequestOptions
    ): Promise<operations.DeployAppConfigurationRawResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.DeployAppConfigurationRawRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = payload$.RequestBody;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/deploy")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/yaml",
            Accept: "application/json",
        });

        const context = {
            operationID: "deployAppConfiguration_raw",
            oAuth2Scopes: [],
            securitySource: null,
        };

        const request$ = this.createRequest$(
            context,
            {
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$ as BodyInit,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.DeployAppConfigurationRawResponse>()
            .json(202, operations.DeployAppConfigurationRawResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.DeployAppConfigurationRawResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get the current run of an app
     */
    async readCurrentRun(
        request: operations.ReadCurrentRunRequest,
        options?: RequestOptions
    ): Promise<operations.ReadCurrentRunResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadCurrentRunRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/run")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = { operationID: "readCurrentRun", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadCurrentRunResponse>()
            .json(200, operations.ReadCurrentRunResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadCurrentRunResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get a specific version
     */
    async readVersion(
        request: operations.ReadVersionRequest,
        options?: RequestOptions
    ): Promise<operations.ReadVersionResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadVersionRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/versions/{id}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = { operationID: "readVersion", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadVersionResponse>()
            .json(200, operations.ReadVersionResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadVersionResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get the run of a version
     */
    async readRun(
        request: operations.ReadRunRequest,
        options?: RequestOptions
    ): Promise<operations.ReadRunResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadRunRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/runs/{id}")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = { operationID: "readRun", oAuth2Scopes: [], securitySource: null };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadRunResponse>()
            .json(200, operations.ReadRunResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadRunResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get logs of the current run of an app
     */
    async readCurrentRunLogs(
        request: operations.ReadCurrentRunLogsRequest,
        options?: RequestOptions
    ): Promise<operations.ReadCurrentRunLogsResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ReadCurrentRunLogsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/run/logs")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = {
            operationID: "readCurrentRunLogs",
            oAuth2Scopes: [],
            securitySource: null,
        };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ReadCurrentRunLogsResponse>()
            .json(200, operations.ReadCurrentRunLogsResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.ReadCurrentRunLogsResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Get the current version of an app
     */
    async getCurrentAppVersion(
        request: operations.GetCurrentAppVersionRequest,
        options?: RequestOptions
    ): Promise<operations.GetCurrentAppVersionResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.GetCurrentAppVersionRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/version")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const context = {
            operationID: "getCurrentAppVersion",
            oAuth2Scopes: [],
            securitySource: null,
        };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.GetCurrentAppVersionResponse>()
            .json(200, operations.GetCurrentAppVersionResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .json("default", operations.GetCurrentAppVersionResponse$inboundSchema)
            .match(response);

        return result$;
    }

    /**
     * Download a specific version of an app
     */
    async downloadAppVersion(
        request: operations.DownloadAppVersionRequest,
        options?: RequestOptions & { acceptHeaderOverride?: DownloadAppVersionAcceptEnum }
    ): Promise<operations.DownloadAppVersionResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.DownloadAppVersionRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;

        const pathParams$ = {
            id: encodeSimple$("id", payload$.id, { explode: false, charEncoding: "percent" }),
        };
        const path$ = this.templateURLComponent("/apps/{id}/version/download")(pathParams$);

        const query$ = "";

        const headers$ = new Headers({
            Accept: options?.acceptHeaderOverride || "application/json;q=1, application/gzip;q=0",
        });

        const context = {
            operationID: "downloadAppVersion",
            oAuth2Scopes: [],
            securitySource: null,
        };

        const request$ = this.createRequest$(
            context,
            {
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.DownloadAppVersionResponse>()
            .stream(200, operations.DownloadAppVersionResponse$inboundSchema, {
                ctype: "application/gzip",
            })
            .fail(["4XX", "5XX"])
            .json("default", operations.DownloadAppVersionResponse$inboundSchema)
            .match(response);

        return result$;
    }
}
